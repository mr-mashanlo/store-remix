import { MetaFunction } from '@remix-run/react';

const meta: MetaFunction = () => {
  return [
    { title: 'Lee — Shop Quality Products Online at the Best Prices' },
    { name: 'description', content: 'Discover a wide selection of high-quality products at unbeatable prices' }
  ];
};

export default meta;